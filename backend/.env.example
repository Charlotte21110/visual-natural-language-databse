# 服务端口
PORT=3001

# 大模型配置（通义千问/DeepSeek/OpenAI 兼容）
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=your_api_key_here
LLM_MODEL=qwen3-max
# ChromaDB 配置
CHROMA_HOST=localhost
CHROMA_PORT=8000

# 存储配置（选择一种）
# memory: 内存存储（默认，适合开发）
# file: 文件存储（适合单机部署，数据持久化）
# redis: Redis 存储（推荐生产环境）
STORAGE_TYPE=memory

# 文件存储路径（仅当 STORAGE_TYPE=file 时使用）
STORAGE_FILE_PATH=./data/storage.json

# Redis 配置（仅当 STORAGE_TYPE=redis 时使用）
# REDIS_URL=redis://localhost:6379

# CAPI 配置（用于需要 Cookie 的高级操作，如修改字段、创建数据模型）
# ⚠️ Cookie 不要放环境变量！由前端登录后调用 POST /api/auth/login 传递
# 后端会存储在内存/Redis 中
CAPI_BASE_URL=https://tcb.cloud.tencent.com/dev

# 开发模式
NODE_ENV=development
